(transfer-policy) {
    tsig {
        secrets /etc/rilldns/transfer.keys
        require AXFR IXFR
    }
    transfer {
        # TSIG is mandatory; wildcard avoids CoreDNS auto's synchronous NOTIFY
        # callback. RillDNS sends NOTIFY after local publication verification.
        to *
    }
}
