[Service]
# Remote configuration sync is disabled by default. If enabled, expose the
# endpoint through an authenticated HTTPS reverse proxy; plaintext HTTP and
# HTTPS-to-HTTP redirects are rejected by the client.
# Environment=RILLDNS_BLOCKLIST_CONFIG_URL=https://dns.example.net/v1/blocklists/config
